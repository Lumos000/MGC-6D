# Experimental Entry Points

The maintained first-release path is kept at the repository root:

- `anchor_paper.py`
- `query_paper.py`

Legacy research scripts, dataset-specific prototypes, and earlier ablation
variants are preserved under `experiments/legacy/`. They are useful for
reference, but many still contain lab-machine defaults and should be cleaned
before being presented as reproducible public commands.
